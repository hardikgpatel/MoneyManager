class WalletModel {
  final String id;
  final int amount;
  final DateTime createdDate;
  final bool isDefault;

  WalletModel({
    this.id,
    this.amount,
    this.createdDate,
    this.isDefault,
  });
}
